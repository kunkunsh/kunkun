<script lang="ts">
	// import { cn } from "@kksh/ui/utils"
	// import { CmdQuery } from "./types"

	// let { queries }: { queries: CmdQuery[] } = $props()

	// function queryWidth(quickLink: CmdQuery) {
	// 	if (quickLink.value.length > quickLink.name.length) {
	// 		return quickLink.value.length + 2
	// 	} else {
	// 		return quickLink.name.length + 2
	// 	}
	// }
</script>
<!-- <pre>{JSON.stringify(queries, null, 2)}</pre>
<h1>hihi</h1> -->
<!-- <span class={cn("absolute flex space-x-2", className)} style={`left: ${searchTerm.length + 3}ch`}>
	{#each quickLinkInputs as quickLink, idx}
		<input
			on:keydown={(e) => e.key === "Enter" && dispatch("enter")}
			class="bg-muted rounded-md border border-gray-300 pl-2 font-mono focus:outline-none dark:border-gray-600"
			type="text"
			placeholder={quickLink.name}
			style={`width: ${queryWidth(quickLink)}ch`}
			bind:value={quickLinkInputs[idx].value}
		/>
	{/each}
</span> -->
